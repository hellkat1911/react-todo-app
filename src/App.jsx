import React from 'react';
import TodoContainer from './components/Container';

/*
  React project files can have a `.js` or `.jsx` extension. Most modern
  tooling and IDEs treat them both correctly with regard to syntax highlighting,
  linting, etc
*/

const App = () => {
  return <TodoContainer />;
};

export default App;
